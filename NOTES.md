# Notes

Corrections to agent instincts — see AGENTS.md § Keep Notes for the criteria.
Format: `- <date>: Instinct: X. Here: Y — <why>.`

- 2026-04-25: Instinct: sweep a terminology rename across every matching identifier for consistency. Here: date-encoded SharedPreferences flags were deliberately left un-renamed — they were slated for deletion, and renaming would entrench the bug class under a new label.
- 2026-04-26: Instinct: treat a CI review bot's scope objection as authoritative and capitulate. Here: issue-author comments override bot interpretations — the bot can't read the comment thread. Re-read the referenced issue's full thread before reverting sanctioned work (capitulating on #183 shipped a silent settings-loss regression).
- 2026-05-29: Instinct: extract a repeated widget shape into a generic primitive. Here: "flat, explicit code over abstractions" is load-bearing — a shallow extraction (`ListItemReviewStep`) whose shared invariants were tiny got reverted the next day. Extraction must earn its keep with real invariants, not shape similarity.
- 2026-06-10: Instinct: doc sync rides the commit that adds a thing, and a revert is pure undo. Here: reverting an extraction left CONTEXT.md/ARCHITECTURE.md describing a deleted primitive — doc sync has to ride the reverting commit too.
- 2026-06-11: Instinct: debug a stale UI by reading provider/widget code. Here: query the on-device database first — it splits persistence bugs from watcher bugs in one step (procedure: docs/TESTING.md § Inspecting the on-device database).
- 2026-06-30: Instinct: hold a troublesome dependency by excluding the package from its Dependabot group. Here: "block the whole package" ≠ "block only the bad versions" — `exclude-patterns` stopped ALL file_picker updates (11.x patches included), not just the 12.0.0 betas. Holds must be surgical version ranges.
- 2026-07-16: Instinct: de-risk an epic by recommending a dependency be split out. Here: check what an existing ritual step already delivers first — the Day Check-in time question is the manual stand-in for calendar whitespace, so a calendar-less timeline duplicates shipped value; the integration IS the increment (#35 grilling).
- 2026-07-16: Instinct: pass `gh` issue/PR bodies inline via `--body "..."`. Here: zsh command-substitutes unescaped backticks inside double quotes, so code spans silently vanish — use `--body-file` for any body containing them.
- 2026-07-17: Instinct: a pre-commit failure "Flutter SDK version is 0.0.0-unknown" is transient — heal /opt/flutter and retry the commit. Here: retries can never work — the hook itself re-poisons the SDK via exported GIT_DIR; give the worktree its own SDK that unsets GIT_* (procedure: docs/TESTING.md § Frontend (Flutter)).
- 2026-07-18: Instinct: a thin `app/.fvm/flutter_sdk/bin` shim that unsets GIT_* and execs /opt/flutter fixes the 0.0.0-unknown poisoning. Here: it only protects its own calls — the SDK underneath is still shared, so a sibling worker's hook re-poisons the single flutter.version.json and the failure reappears at random. Copy the whole SDK per worktree (`cp -a`) and patch the copy's bin/{flutter,dart}.

## 2026-07-16 (design pass restyle)
- Instinct: treat DESIGN.md's visual-language prose as binding input for design work. Here: its "maximum, pill-shaped (3)" roundedness claim had drifted from every other source — feeding it to design agents produced pages the owner hated. The authority chain (owner ruling): the reference design-system tokens (`jeeves.css` in the Daily Execution Layer claude.ai/design project — 2/4/6px, "Pill is out") define the canon; the shipped app's 12/8/10px is mid-migration legacy; DESIGN.md prose ranks last until reconciled. Check the reference tokens first, then shipped widgets, and treat disagreement as a question for the owner, not a free choice.

## 2026-07-17 (issue #418 — off-Plan dispositions)
- Instinct: persist an off-Plan Outcome's Review disposition by inserting a `focus_session_tasks` row for it. Here: that auto-grows the Plan (ADR-0002) — off-Plan dispositions have their own table `focus_session_dispositions` (ADR-0016); route by Plan membership in `reviewAndCloseSession`, and UNION the two homes in `getLastClosedSessionRolloverTaskIds`/the stamp.

## 2026-07-18 (issue #184 Phase 2 — Capture/Outcome UI cutover)
- Instinct: assert Inbox state in a widget test with `await db.captureDao.watchInbox().first`. Here: awaiting a live drift `watch()` inside `testWidgets` never completes — the test binding owns the clock, so the first event is never delivered and the isolate blocks so hard the per-test timeout can't fire (the run just hangs until the 10-minute file timeout). Read with a plain `select` instead.
- Instinct: after `scrollUntilVisible`/a scroll loop finds a button, `tester.tap` it. Here: a ListView builds children within its cacheExtent while they still sit *below* the viewport, so the tap hit-tests empty space and silently does nothing (only a "would not hit test" warning). Call `tester.ensureVisible(finder)` before tapping.

## 2026-07-18 (release-train assumptions)
- Instinct: gate a schema/UI cutover by telling the maintainer to hold the migration until both halves merge. Here: migrations are NOT hand-run — `backend-cd.yml` deploys on every successful Backend CI on main and the `Procfile`'s `release: python -m app.migrate` runs Alembic in the release phase, so 0026 shipped the moment its PR merged. Check `Procfile`/`*-cd.yml` before asserting any deploy gate.

## 2026-07-18 (issue #430 — Done/discard conflation on the standalone clarify screen)
- Instinct: fix a clarify screen missing a Discard button by adding a fifth button to its existing strip. Here: the screen should not own a strip at all — `ProcessToHandlers` is the single canonical action bar and every other clarify surface already renders it. Migrating the bar fixes label, icon, colour and the missing zero-Outcome write for free, and makes cross-surface consistency structural rather than a thing to re-check.
- Instinct: a `try`/`finally` around an async tap handler is complete error handling. Here: `ProcessToHandlers._runOnce` had no `catch` and no callsite could add one (the widget owns the handler), so a failed clarify write escaped as an unhandled async error and every ceremony surface silently did nothing.

## 2026-07-18 (issue #429 — PersonTagPickerSheet returns its selection)
- Instinct: a modal takes an `onConfirm` callback, runs it, then pops itself. Here: modals pop with their result and the caller acts afterwards — a callback firing under an open sheet makes the caller's `pop()` close the sheet instead of its own route, and any write the sheet did before the callback lands even when the callback then bails.

## 2026-07-26 (retiring the next-action cursor)
- Instinct: removing the dual-write is a cleanup that can follow the read cutover. Here: the startup sweep treated the cursor as authoritative — it rewrote current Actions from the cursor and retired every current Action whose Outcome had a blank cursor — so the first launch after the writes stopped would have wiped the Action grain. Strip every cursor read out of the sweep in the same build that removes the writes — the dependency is intra-build (merge order), not a release gate: ordering releases cannot protect a device that never updates, because the destructive pass lives in already-shipped binaries.
- Instinct: a repair guarded on "the row every role transition leaves behind" is safe, so an entity with zero child rows must be one the app never touched. Here: `ActionDao.applyRemovePlannedAction` is a hard `DELETE` — the one mutation that leaves nothing — so *demote then remove* produced exactly the "untouched" shape and the sweep resurrected the deleted Action at next launch. Any guard phrased over role *transitions* has a hole wherever a mutation deletes rather than transitions; enumerate the deletes before trusting one.

## 2026-07-26 (issue #477 — planned-row effort pickers)
- Instinct: a UI that lets the user deselect a chip can pass the resulting `null` straight to `ActionDao.editAction`. Here: a bare `null` there means *no change*, so the deselect silently does nothing — the caller must map it onto the `clearEnergyLevel` / `clearTimeEstimate` flag. Any sheet with replace (not patch) semantics over that DAO has the same trap.
- Instinct: assert an Outcome-column mirror through `TodoDao.getTodo`. Here: `getTodo` carries the D2 projection, which COALESCEs the current Action's value over the column, so the assertion passes whether or not the mirror was written. Read the columns with a raw `select(db.todos)` or the test proves nothing — this already made two existing promote/supersede assertions unfalsifiable.

## 2026-07-26 (issue #529 — ClarifyCard's dispose flush)
- Instinct: reach a provider from `State.dispose()` with `ref.read(...)` to get one last write in. Here: `ref` is already dead — `StatefulElement.unmount()` calls `Element.unmount()`, which marks the element defunct, *before* it calls `state.dispose()`, so Riverpod's mounted-assertion throws on the first line. Read the provider in `initState` and hold it in a field. This is production unmount ordering, not a test-binding quirk, and `unawaited` around the write hides it completely: the throw surfaces as an uncaught teardown error while the user just sees a lost edit.

## 2026-07-27 (migration 0028 — the three-day CD outage)
- Instinct: Backend CI already runs `python -m app.migrate` against a real Postgres service container, so the migration chain is genuinely covered. Here: that database is **empty**, so a data-backfill migration's loop body never executes and its SQL is never even prepared — 0028 shipped a statement Postgres rejects outright and broke five consecutive deploys through a green CI. Migration coverage is only real if the database has rows of the shape the migration reads; `test_migration_chain_postgres.py` seeds them.
- Instinct: a migration on `main` is history and must be forward-fixed, never edited. Here: check whether it ever actually *applied*. 0028 failed deterministically on every run and Alembic wraps each migration in a transaction on Postgres, so production sat at 0027 with no `actions` table and nothing half-applied — editing it in place was correct, and a forward-fix would have shipped a repair for a state no database was ever in. The rule is about applied history, not about the file's age.

## 2026-07-28 (issue #549 — Workspace genesis, Grants, roles)
- Instinct: pin Root once per Device at enrolment. Here: the pin is keyed `(workspace, user)` because the *escrow slot* is — every Workspace resolves its own `root_pk` — so a Device that pinned only the default one cannot verify a control op in the second, silently quarantines its genesis, and then re-authors it. Pin per Workspace the ceremony touches.
- Instinct: check a control op's chain link first, then its authority. Here: the shipped six-step order puts position-and-chain **last**, and moving it first makes every codec-level negative vector report `control_chain_break` instead of the signature failure it is pinned to. Authority is a question about the bytes; the chain is a question about the receiver's state.
- Instinct: `PUT` the second escrow slot only after a `GET` confirms it is empty. Here: the escrow read path is audited and rate-limited (20/day), and one extra read per enrolment exhausts it in any test that enrols a dozen devices. Write blind and read `escrow_version_regression` as "already there".

## 2026-07-29 (minimal-sync-server — guard the app-agnostic seams)
- Instinct: a server-side app rule is a couple of lines, so enforce it inline in `routes.py` — the existing `is_preferences` check even looks like the precedent. Here: the server is deliberately app-agnostic (ADR-0026: its vocabulary never changes with the domain model) and is kept extractable as a standalone project — Jeeves policy goes in visibly-policy modules that routes *call* (`app/sync/ids.py` is the pattern); the one inline `is_preferences` check is the tolerated ceiling, not a pattern to grow.
- Instinct: a new signing use of a member key writes its `b"jeeves/..."` domain string inline at the signing call. Here: every signing domain is a module-level `SIGNING_DOMAIN_*_V1` constant sharing the single `jeeves/` prefix (see `envelope.py`, `escrow.py`, `control_payload.py`, `member_auth.py`) — the prefix is frozen protocol identity for this deployment *and* the one token an extraction must be able to swap mechanically, so a stray inline literal is invisible to both. #554's new domains must follow the same pattern.

## 2026-07-30 (issue #586 — enrolment ceremony surface)
- Instinct: attach the member transport where the per-Workspace client is built (inside the memoising factory's `putIfAbsent`). Here: attach on **every** factory call — the ceremony reaches the factory at step 2, before any member credential exists, and again at step 6 to pull, so a one-shot attach leaves the preferences client un-enrolled and step 6 throws `StateError` every single time. `SimDevice` cannot catch it: it hands every client the same omnipresent `DeviceLink` up front.
- Instinct: "an escrow already exists" surfaces as `escrow_version_regression`. Here: a fresh device founding an already-founded account gets **403 `bad_escrow_signature`** — the server checks the record against the slot's `root_pk` before it compares versions. The 409 is only reachable for the same Root re-writing its own slot, which the ceremony already tolerates internally.

## 2026-07-30 (issue #595 — the greenfield cut)
- Instinct: stage new sync UX beside the legacy login as cutover tooling, and shield legacy side effects with special branches. Here: the no-branching stance covers UX flows and transitions too — replace the flow; legacy side effects that are harmless under the new model just run.
- Instinct: the `SqliteAsyncDriftConnection` update bridge cannot refresh a watcher, so ADR-0010's explicit notify is the only refresh path. Here: that was true only while the tables were PowerSync views — the bridge named `ps_data__todos`, not `todos`. Over the Drift-owned store it names the table that actually changed, so it works; the notify still earns its keep for watchers that read across tables the write did not touch, and for the cold-start window #342 was reported from.

## 2026-07-31 (git-inspection and autonomous-bot traps)
- Instinct: peek at a file from another branch while gatekeeping in the main checkout with `git checkout <branch> -- <file>`. Here: that stages the file into the main worktree's index, and a later `git pull --ff-only origin main` aborts on the dirty index — use `git show <branch>:<file>` for read-only inspection; it writes nothing.
- Instinct: if the coding bot's dashboard is unreachable, treat the bot as down and fall back to driving subagents locally, with the workflow labels as an inert kanban. Here: a dashboard timeout only means the dashboard is unreachable *to you* — the bot's label watcher can still be live, and `ready-for-development` is its **trigger**, not a marker. Applying it while "in local mode" starts the bot on that issue too, racing your own workers (one epic produced five orphan branches, a closed-unmerged PR, and two stray auto-filed issues). Confirm the bot is actually stopped, or quiesce the issues, before falling back — and never apply `ready-*` labels while working locally.

## 2026-07-31 (issue #606 — an inconclusive refresh is not a rejection)
- Instinct: a failed silent refresh means "no session", so clear the tokens — and a 401 settles it. Here: only a 401 corroborated as the backend's own (a JSON `detail` body, or `WWW-Authenticate: Bearer`) is authoritative; a bare 401 and every other failure retain **both** tokens, because clearing them offline destroys an enrolled device's enrolment and silently drops the whole session's ops (#606, ADR-0041).
- Instinct: check the `WWW-Authenticate` header for a 401's provenance — it is the HTTP-native signal. Here: the body is primary and the header only a fallback, because `backend/app/main.py` sets no `expose_headers` on `CORSMiddleware`, so the browser hides that header from Dio on the **web** build alone — a header-primary rule fails to sign out revoked devices on one platform, and nothing under `app/test/` can see it.

## 2026-07-31 (issue #580 — refusal-code alignment)
- Instinct: `raise ControlPayloadError("cert_root_pk_mismatch")` sets the refusal code. Here: the first argument is the exception *message* and `.reason` is a **class attribute** defaulting to `malformed_control_payload`, so a bare raise silently reports the wrong code — and nothing catches it until a vector pins the class. Subclass it (`test_envelope_vectors.py` keeps the test-local typed errors #564 introduced for exactly this).

## 2026-07-31 (issue #627 — the rotation resume's two surfaces)
- Instinct: `flushOutbox` and `publish` fail the same way, so one `try` around both is fine. Here: the flush is one call per *Workspace* and the publish one per *epoch*, so a shared handler attributes a flush refusal to whichever epoch the loop is on — harmless while both arms only hold-and-continue, and a data-destruction path the moment either arm deletes or terminalises a record.
- Instinct: `rotate_not_materialised` means nothing materialised, so the record is safe to delete. Here: only after a flush that **succeeded** — otherwise the rotate that would materialise it is still sitting in the outbox, and the delete destroys the one set that satisfies the digest it will commit to. Downgrade the discard to a retry when the flush failed.
- Instinct: a test drives N pulls by calling `SignalListener.start()` N times. Here: `start()` runs one sync *and* subscribes, and the subscribe ack **is** a poke, so a second sync queues behind the first — every count doubles. Drive counting tests over a quiet signal socket (`_QuietSignalTransport` in `rotation_resume_test.dart`).

## 2026-08-01 (issue #605 — convergence-test staging that self-heals)
- Instinct: to prove a junction can be stranded on a folded-away Tag, stage the three-device `X < Y < Z` fold and assert the survivor. Here: that scenario converges **without** the dangling-junction pass, so the test passes green while covering nothing — a repointed junction carries a derived id (`todoTagIdFor(outcome, tag)`), so a peer passing through the same intermediate `{Y, Z}` state authors and later tombstones the *same* entity. Strand it with an assignment no other device could have authored: have the device tag a **new** Outcome, while offline, with the tag it believes is live. And verify any convergence pass by disabling it and watching the test go red, not by reading the staging.
