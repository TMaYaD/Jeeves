name: jeeves
description: A productivity-focused todos app for mobile, web, and desktop.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2

  # Local storage (offline-first)
  drift: ^2.31.0
  # Bridges Drift to the sqlite_async connection that PowerSync owns, so one
  # shared SQLite file backs both.  Without this, Drift and PowerSync would
  # each open their own connection and their watchers would never see each
  # other's writes.
  drift_sqlite_async: ^0.2.6
  # sqlite_async itself is a transitive dependency of both powersync and
  # drift_sqlite_async, but imported directly in gtd_database.dart for the
  # `SqliteConnection` type in the production constructor signature.
  sqlite_async: ^0.13.1
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.3
  path: ^1.9.0

  # HTTP / API
  dio: ^5.4.3
  retrofit: ^4.9.2

  # Sync (PowerSync — bidirectional offline-first sync)
  # Held at 1.x: powersync 2.0 requires sqlite3 ^3.2.0 → drift_dev 2.32+
  # → analyzer ^10, which conflicts with riverpod_generator 4.0.3
  # (latest, pinned to analyzer ^9). Revisit when riverpod_generator
  # releases an analyzer-10-compatible version.
  powersync: ^1.0.0

  # Notifications
  flutter_local_notifications: ^21.0.0
  flutter_timezone: ^5.0.0
  timezone: ^0.11.0
  # firebase_messaging: ^16.1.3   # Uncomment when Firebase is configured
  # firebase_core: ^4.6.0         # Uncomment when Firebase is configured

  # Location
  geolocator: ^14.0.2
  geocoding: ^4.0.0

  # Voice / Speech
  speech_to_text: ^7.3.0

  # AI / NLP helpers
  # (calls backend AI endpoint via api_service)

  # Navigation
  go_router: ^17.2.1

  # Connectivity
  connectivity_plus: ^7.1.1

  # Local key-value persistence (planning ritual state)
  shared_preferences: ^2.3.3

  # File import
  file_picker: ^8.1.2

  # Encrypted token storage (JWT)
  flutter_secure_storage: ^10.0.0

  # Utilities
  uuid: ^4.4.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0
  intl: ^0.20.2
  google_fonts: ^8.0.2
  flutter_markdown_plus: ^1.0.7
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  sqlite3: ^2.9.4
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  freezed: ^3.2.5
  json_serializable: ^6.13.0
  riverpod_generator: ^4.0.3
  drift_dev: ^2.31.0
  retrofit_generator: ^10.2.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
