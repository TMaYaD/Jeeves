/// Shared user-identity constants used across providers.
///
/// Keeping this in a neutral module prevents unrelated providers from
/// coupling to the inbox feature file.
library;

/// Placeholder user id until authentication is wired up.
const kLocalUserId = 'local';
