# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# Code generation (regenerate with `dart run build_runner build`)
*.freezed.dart
*.g.dart
*.gr.dart

# IDE
.idea/
*.iml
.vscode/

# FVM (Flutter Version Management) — local SDK symlink, not the pinned version itself
.fvm/

# macOS
.DS_Store

# Web assets (downloaded at build time by tool/fetch_web_assets.sh via `make setup`)
# Never commit these — they must match the pinned powersync version in pubspec.lock.
web/sqlite3.wasm
web/powersync_db.worker.js
