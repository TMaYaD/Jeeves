SCRATCHPAD.md
.claude/worktrees/
# FVM's local SDK materialisation (machine-specific, never committed). `fvm use`
# creates app/.fvm/flutter_sdk pointing at the version pinned in app/.fvmrc;
# linked worktrees don't get one, and .githooks/pre-commit falls back to the
# main checkout's.
app/.fvm/
__pycache__/
